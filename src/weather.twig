<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    {% if(cityId) %}
      <p>The weather of {{ data.name }} at {{ date }} is {{ data.weather[0].main }}</p>
      <p><a href="weather.php">Try again</a></p>
    {% else %}
    <form class="" action="weather.php" method="GET">
      <select class="" name="city_id">
        {% for c in cities %}
          <option value="{{ c.id }}">{{ c.name }}</option>
        {% endfor  %}
      </select>
      <button type="submit" name="action" value="1">Go !</button>
    </form>
    {% endif %}
  </body>
</html>
